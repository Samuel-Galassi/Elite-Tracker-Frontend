import styled from 'styled-components';

export const Container = styled.div`

    h1 {
        font-size: 24px;
        font-weight: 600;
        color: var(--white);
        margin-bottom: 14px;
    }

    span {
        font-size: 14px;
        font-weight: 400;
        color: var(--white);
    }
`;
